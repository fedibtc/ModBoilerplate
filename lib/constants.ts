export const RELAYS = ["wss://relay.snort.social", "wss://nos.lol"];

/** 
 * Determines the maximum number of milliseconds a pending invoice can last before being rejected.
 * Defaults to an hour.
 */
export const invoiceVerificationTimeout = 3600000;